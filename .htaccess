<IfModule mod_rewrite.c>
    RewriteEngine On

    # 1. Paksa masuk ke jalur HTTPS (Gembok)
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

    # 2. Arahkan ke folder public Laravel
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>